/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Injectable } from '@angular/core';
import { AspNetUserTextModel } from './aspnetuser.models';
import { BehaviorSubject, of } from 'rxjs';
import { LoadLocalesAspNetUserText } from './aspnetuser.locales';
import { Router } from '@angular/router';
import { HttpClient, HttpErrorResponse, HttpHeaders } from '@angular/common/http';
import { map, catchError } from 'rxjs/operators';
import { AspNetUser } from '../../../models/generated/AspNetUser.model';
import { HttpRequestModel } from '../../../models/http.model';

@Injectable({
  providedIn: 'root'
})
export class AspNetUserService {
  /* Variables */
  aspnetuserTextModel$: BehaviorSubject<AspNetUserTextModel> = new BehaviorSubject<AspNetUserTextModel>(<AspNetUserTextModel>{});
  aspnetuserListModel$: BehaviorSubject<AspNetUser[]> = new BehaviorSubject<AspNetUser[]>(<AspNetUser[]>{});
  aspnetuserGetModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  aspnetuserPutModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  aspnetuserPostModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  aspnetuserDeleteModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  aspnetuserList: AspNetUser[] = [];
  private oldURL: string;
  private router: Router;

  /* Constructors */
  constructor(private httpClient: HttpClient) {
    LoadLocalesAspNetUserText(this);
    this.aspnetuserTextModel$.next(<AspNetUserTextModel>{ Title: "Something2 for text" });
  }

  /* Functions public */
  GetAspNetUserList(router: Router) {
    this.BeforeHttpClient(this.aspnetuserGetModel$, router);

    return this.httpClient.get<AspNetUser[]>('/api/AspNetUser').pipe(
      map((x: any) => {
        this.DoSuccess(this.aspnetuserGetModel$, x, 'Get', null);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.aspnetuserGetModel$, e, 'Get');
      })))
    );
  }

  PutAspNetUser(aspnetuser: AspNetUser, router: Router) {
    this.BeforeHttpClient(this.aspnetuserPutModel$, router);

    return this.httpClient.put<AspNetUser>('/api/AspNetUser', aspnetuser, { headers: new HttpHeaders() }).pipe(
      map((x: any) => {
        this.DoSuccess(this.aspnetuserPutModel$, x, 'Put', aspnetuser);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.aspnetuserPutModel$, e, 'Put');
      })))
    );
  }

  PostAspNetUser(aspnetuser: AspNetUser, router: Router) {
    this.BeforeHttpClient(this.aspnetuserPostModel$, router);

    return this.httpClient.post<AspNetUser>('/api/AspNetUser', aspnetuser, { headers: new HttpHeaders() }).pipe(
      map((x: any) => {
        this.DoSuccess(this.aspnetuserPostModel$, x, 'Post', aspnetuser);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.aspnetuserPostModel$, e, 'Post');
      })))
    );
  }

  DeleteAspNetUser(aspnetuser: AspNetUser, router: Router) {
    this.BeforeHttpClient(this.aspnetuserDeleteModel$, router);

    return this.httpClient.delete<boolean>(`/api/AspNetUser/${ aspnetuser.AspNetUserID }`).pipe(
      map((x: any) => {
        this.DoSuccess(this.aspnetuserDeleteModel$, x, 'Delete', aspnetuser);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.aspnetuserDeleteModel$, e, 'Delete');
      })))
    );
  }

  /* Functions private */
  private BeforeHttpClient(httpRequestModel$: BehaviorSubject<HttpRequestModel>, router: Router) {
    this.router = router;
    this.oldURL = router.url;
    httpRequestModel$.next(<HttpRequestModel>{ Working: true, Error: null, Status: null });
  }

  private DoCatchError(httpRequestModel$: BehaviorSubject<HttpRequestModel>, e: any, command: string) {
    this.aspnetuserListModel$.next(null);
    httpRequestModel$.next(<HttpRequestModel>{ Working: false, Error: <HttpErrorResponse>e, Status: 'Error' });

    this.aspnetuserList = [];
    console.debug(`AspNetUser ${ command } ERROR. Return: ${ <HttpErrorResponse>e }`);
    this.DoReload();
  }

  private DoReload() {
    this.router.navigateByUrl('', { skipLocationChange: true }).then(() => {
      this.router.navigate([`/${this.oldURL}`]);
    });
  }

  private DoSuccess(httpRequestModel$: BehaviorSubject<HttpRequestModel>, x: any, command: string, aspnetuser?: AspNetUser) {
    console.debug(`AspNetUser ${ command } OK. Return: ${ x }`);
    if (command === 'Get') {
      this.aspnetuserListModel$.next(<AspNetUser[]>x);
    }
    if (command === 'Put') {
      this.aspnetuserListModel$.getValue()[0] = <AspNetUser>x;
    }
    if (command === 'Post') {
      this.aspnetuserListModel$.getValue().push(<AspNetUser>x);
    }
    if (command === 'Delete') {
      const index = this.aspnetuserListModel$.getValue().indexOf(aspnetuser);
      this.aspnetuserListModel$.getValue().splice(index, 1);
    }

    this.aspnetuserListModel$.next(this.aspnetuserListModel$.getValue());
    httpRequestModel$.next(<HttpRequestModel>{ Working: false, Error: null, Status: 'ok' });
    this.aspnetuserList = this.aspnetuserListModel$.getValue();
    this.DoReload();
  }
}
