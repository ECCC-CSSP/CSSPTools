/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { ContactModelService } from './contactmodel.service';
import { LoadLocalesContactModelText } from './contactmodel.locales';
import { Subscription } from 'rxjs';
import { ContactModel } from '../../../models/generated/ContactModel.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-contactmodel',
  templateUrl: './contactmodel.component.html',
  styleUrls: ['./contactmodel.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class ContactModelComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public contactmodelService: ContactModelService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(contactmodel: ContactModel) {
    if (this.IDToShow === contactmodel.ContactModelID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(contactmodel: ContactModel) {
    if (this.IDToShow === contactmodel.ContactModelID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(contactmodel: ContactModel) {
    if (this.IDToShow === contactmodel.ContactModelID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = contactmodel.ContactModelID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(contactmodel: ContactModel) {
    if (this.IDToShow === contactmodel.ContactModelID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = contactmodel.ContactModelID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetContactModelList() {
    this.sub = this.contactmodelService.GetContactModelList().subscribe();
  }

  DeleteContactModel(contactmodel: ContactModel) {
    this.sub = this.contactmodelService.DeleteContactModel(contactmodel).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesContactModelText(this.contactmodelService.contactmodelTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
