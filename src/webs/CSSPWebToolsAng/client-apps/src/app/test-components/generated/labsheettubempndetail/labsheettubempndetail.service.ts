/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Injectable } from '@angular/core';
import { LabSheetTubeMPNDetailTextModel } from './labsheettubempndetail.models';
import { BehaviorSubject, of } from 'rxjs';
import { LoadLocalesLabSheetTubeMPNDetailText } from './labsheettubempndetail.locales';
import { Router } from '@angular/router';
import { HttpClient, HttpErrorResponse, HttpHeaders } from '@angular/common/http';
import { map, catchError } from 'rxjs/operators';
import { LabSheetTubeMPNDetail } from '../../../models/generated/LabSheetTubeMPNDetail.model';
import { HttpRequestModel } from '../../../models/http.model';

@Injectable({
  providedIn: 'root'
})
export class LabSheetTubeMPNDetailService {
  /* Variables */
  labsheettubempndetailTextModel$: BehaviorSubject<LabSheetTubeMPNDetailTextModel> = new BehaviorSubject<LabSheetTubeMPNDetailTextModel>(<LabSheetTubeMPNDetailTextModel>{});
  labsheettubempndetailListModel$: BehaviorSubject<LabSheetTubeMPNDetail[]> = new BehaviorSubject<LabSheetTubeMPNDetail[]>(<LabSheetTubeMPNDetail[]>{});
  labsheettubempndetailGetModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  labsheettubempndetailPutModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  labsheettubempndetailPostModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  labsheettubempndetailDeleteModel$: BehaviorSubject<HttpRequestModel> = new BehaviorSubject<HttpRequestModel>(<HttpRequestModel>{});
  labsheettubempndetailList: LabSheetTubeMPNDetail[] = [];
  private oldURL: string;
  private router: Router;

  /* Constructors */
  constructor(private httpClient: HttpClient) {
    LoadLocalesLabSheetTubeMPNDetailText(this);
    this.labsheettubempndetailTextModel$.next(<LabSheetTubeMPNDetailTextModel>{ Title: "Something2 for text" });
  }

  /* Functions public */
  GetLabSheetTubeMPNDetailList(router: Router) {
    this.BeforeHttpClient(this.labsheettubempndetailGetModel$, router);

    return this.httpClient.get<LabSheetTubeMPNDetail[]>('/api/LabSheetTubeMPNDetail').pipe(
      map((x: any) => {
        this.DoSuccess(this.labsheettubempndetailGetModel$, x, 'Get', null);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.labsheettubempndetailGetModel$, e, 'Get');
      })))
    );
  }

  PutLabSheetTubeMPNDetail(labsheettubempndetail: LabSheetTubeMPNDetail, router: Router) {
    this.BeforeHttpClient(this.labsheettubempndetailPutModel$, router);

    return this.httpClient.put<LabSheetTubeMPNDetail>('/api/LabSheetTubeMPNDetail', labsheettubempndetail, { headers: new HttpHeaders() }).pipe(
      map((x: any) => {
        this.DoSuccess(this.labsheettubempndetailPutModel$, x, 'Put', labsheettubempndetail);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.labsheettubempndetailPutModel$, e, 'Put');
      })))
    );
  }

  PostLabSheetTubeMPNDetail(labsheettubempndetail: LabSheetTubeMPNDetail, router: Router) {
    this.BeforeHttpClient(this.labsheettubempndetailPostModel$, router);

    return this.httpClient.post<LabSheetTubeMPNDetail>('/api/LabSheetTubeMPNDetail', labsheettubempndetail, { headers: new HttpHeaders() }).pipe(
      map((x: any) => {
        this.DoSuccess(this.labsheettubempndetailPostModel$, x, 'Post', labsheettubempndetail);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.labsheettubempndetailPostModel$, e, 'Post');
      })))
    );
  }

  DeleteLabSheetTubeMPNDetail(labsheettubempndetail: LabSheetTubeMPNDetail, router: Router) {
    this.BeforeHttpClient(this.labsheettubempndetailDeleteModel$, router);

    return this.httpClient.delete<boolean>(`/api/LabSheetTubeMPNDetail/${ labsheettubempndetail.LabSheetTubeMPNDetailID }`).pipe(
      map((x: any) => {
        this.DoSuccess(this.labsheettubempndetailDeleteModel$, x, 'Delete', labsheettubempndetail);
      }),
      catchError(e => of(e).pipe(map(e => {
        this.DoCatchError(this.labsheettubempndetailDeleteModel$, e, 'Delete');
      })))
    );
  }

  /* Functions private */
  private BeforeHttpClient(httpRequestModel$: BehaviorSubject<HttpRequestModel>, router: Router) {
    this.router = router;
    this.oldURL = router.url;
    httpRequestModel$.next(<HttpRequestModel>{ Working: true, Error: null, Status: null });
  }

  private DoCatchError(httpRequestModel$: BehaviorSubject<HttpRequestModel>, e: any, command: string) {
    this.labsheettubempndetailListModel$.next(null);
    httpRequestModel$.next(<HttpRequestModel>{ Working: false, Error: <HttpErrorResponse>e, Status: 'Error' });

    this.labsheettubempndetailList = [];
    console.debug(`LabSheetTubeMPNDetail ${ command } ERROR. Return: ${ <HttpErrorResponse>e }`);
    this.DoReload();
  }

  private DoReload() {
    this.router.navigateByUrl('', { skipLocationChange: true }).then(() => {
      this.router.navigate([`/${this.oldURL}`]);
    });
  }

  private DoSuccess(httpRequestModel$: BehaviorSubject<HttpRequestModel>, x: any, command: string, labsheettubempndetail?: LabSheetTubeMPNDetail) {
    console.debug(`LabSheetTubeMPNDetail ${ command } OK. Return: ${ x }`);
    if (command === 'Get') {
      this.labsheettubempndetailListModel$.next(<LabSheetTubeMPNDetail[]>x);
    }
    if (command === 'Put') {
      this.labsheettubempndetailListModel$.getValue()[0] = <LabSheetTubeMPNDetail>x;
    }
    if (command === 'Post') {
      this.labsheettubempndetailListModel$.getValue().push(<LabSheetTubeMPNDetail>x);
    }
    if (command === 'Delete') {
      const index = this.labsheettubempndetailListModel$.getValue().indexOf(labsheettubempndetail);
      this.labsheettubempndetailListModel$.getValue().splice(index, 1);
    }

    this.labsheettubempndetailListModel$.next(this.labsheettubempndetailListModel$.getValue());
    httpRequestModel$.next(<HttpRequestModel>{ Working: false, Error: null, Status: 'ok' });
    this.labsheettubempndetailList = this.labsheettubempndetailListModel$.getValue();
    this.DoReload();
  }
}
