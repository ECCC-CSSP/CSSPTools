/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { PreferenceService } from './preference.service';
import { LoadLocalesPreferenceText } from './preference.locales';
import { Subscription } from 'rxjs';
import { Preference } from '../../../models/generated/Preference.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-preference',
  templateUrl: './preference.component.html',
  styleUrls: ['./preference.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class PreferenceComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public preferenceService: PreferenceService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(preference: Preference) {
    if (this.IDToShow === preference.PreferenceID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(preference: Preference) {
    if (this.IDToShow === preference.PreferenceID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(preference: Preference) {
    if (this.IDToShow === preference.PreferenceID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = preference.PreferenceID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(preference: Preference) {
    if (this.IDToShow === preference.PreferenceID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = preference.PreferenceID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetPreferenceList() {
    this.sub = this.preferenceService.GetPreferenceList().subscribe();
  }

  DeletePreference(preference: Preference) {
    this.sub = this.preferenceService.DeletePreference(preference).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesPreferenceText(this.preferenceService.preferenceTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
