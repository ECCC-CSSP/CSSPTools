/*
 * Auto generated from C:\CSSPTools\src\codegen\Tests\_package\netcoreapp3.1\testhost.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { ResetPasswordService } from './resetpassword.service';
import { LoadLocalesResetPasswordText } from './resetpassword.locales';
import { Subscription } from 'rxjs';
import { ResetPassword } from '../../../models/generated/ResetPassword.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-resetpassword',
  templateUrl: './resetpassword.component.html',
  styleUrls: ['./resetpassword.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class ResetPasswordComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public resetpasswordService: ResetPasswordService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(resetpassword: ResetPassword) {
    if (this.IDToShow === resetpassword.ResetPasswordID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(resetpassword: ResetPassword) {
    if (this.IDToShow === resetpassword.ResetPasswordID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(resetpassword: ResetPassword) {
    if (this.IDToShow === resetpassword.ResetPasswordID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = resetpassword.ResetPasswordID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(resetpassword: ResetPassword) {
    if (this.IDToShow === resetpassword.ResetPasswordID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = resetpassword.ResetPasswordID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetResetPasswordList() {
    this.sub = this.resetpasswordService.GetResetPasswordList().subscribe();
  }

  DeleteResetPassword(resetpassword: ResetPassword) {
    this.sub = this.resetpasswordService.DeleteResetPassword(resetpassword).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesResetPasswordText(this.resetpasswordService.resetpasswordTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
