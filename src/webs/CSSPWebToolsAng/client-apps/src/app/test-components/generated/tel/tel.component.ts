/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { TelService } from './tel.service';
import { LoadLocalesTelText } from './tel.locales';
import { Subscription } from 'rxjs';
import { TelTypeEnum_GetIDText } from '../../../enums/generated/TelTypeEnum';
import { Tel } from '../../../models/generated/Tel.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-tel',
  templateUrl: './tel.component.html',
  styleUrls: ['./tel.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class TelComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public telService: TelService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(tel: Tel) {
    if (this.IDToShow === tel.TelID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(tel: Tel) {
    if (this.IDToShow === tel.TelID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(tel: Tel) {
    if (this.IDToShow === tel.TelID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = tel.TelID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(tel: Tel) {
    if (this.IDToShow === tel.TelID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = tel.TelID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetTelList() {
    this.sub = this.telService.GetTelList().subscribe();
  }

  DeleteTel(tel: Tel) {
    this.sub = this.telService.DeleteTel(tel).subscribe();
  }

  GetTelTypeEnumText(enumID: number) {
    return TelTypeEnum_GetIDText(enumID)
  }

  ngOnInit(): void {
    LoadLocalesTelText(this.telService.telTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
