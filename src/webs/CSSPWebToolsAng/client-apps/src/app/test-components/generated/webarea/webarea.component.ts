/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { WebAreaService } from './webarea.service';
import { LoadLocalesWebAreaText } from './webarea.locales';
import { Subscription } from 'rxjs';
import { WebArea } from '../../../models/generated/WebArea.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-webarea',
  templateUrl: './webarea.component.html',
  styleUrls: ['./webarea.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebAreaComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public webareaService: WebAreaService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(webarea: WebArea) {
    if (this.IDToShow === webarea.WebAreaID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(webarea: WebArea) {
    if (this.IDToShow === webarea.WebAreaID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(webarea: WebArea) {
    if (this.IDToShow === webarea.WebAreaID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webarea.WebAreaID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(webarea: WebArea) {
    if (this.IDToShow === webarea.WebAreaID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webarea.WebAreaID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetWebAreaList() {
    this.sub = this.webareaService.GetWebAreaList().subscribe();
  }

  DeleteWebArea(webarea: WebArea) {
    this.sub = this.webareaService.DeleteWebArea(webarea).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesWebAreaText(this.webareaService.webareaTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
