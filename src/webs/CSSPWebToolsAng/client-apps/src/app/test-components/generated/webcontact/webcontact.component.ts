/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { WebContactService } from './webcontact.service';
import { LoadLocalesWebContactText } from './webcontact.locales';
import { Subscription } from 'rxjs';
import { WebContact } from '../../../models/generated/WebContact.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-webcontact',
  templateUrl: './webcontact.component.html',
  styleUrls: ['./webcontact.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebContactComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public webcontactService: WebContactService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(webcontact: WebContact) {
    if (this.IDToShow === webcontact.WebContactID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(webcontact: WebContact) {
    if (this.IDToShow === webcontact.WebContactID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(webcontact: WebContact) {
    if (this.IDToShow === webcontact.WebContactID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webcontact.WebContactID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(webcontact: WebContact) {
    if (this.IDToShow === webcontact.WebContactID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webcontact.WebContactID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetWebContactList() {
    this.sub = this.webcontactService.GetWebContactList().subscribe();
  }

  DeleteWebContact(webcontact: WebContact) {
    this.sub = this.webcontactService.DeleteWebContact(webcontact).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesWebContactText(this.webcontactService.webcontactTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
