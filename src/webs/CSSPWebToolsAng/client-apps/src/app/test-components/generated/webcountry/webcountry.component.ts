/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { WebCountryService } from './webcountry.service';
import { LoadLocalesWebCountryText } from './webcountry.locales';
import { Subscription } from 'rxjs';
import { WebCountry } from '../../../models/generated/WebCountry.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-webcountry',
  templateUrl: './webcountry.component.html',
  styleUrls: ['./webcountry.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebCountryComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public webcountryService: WebCountryService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(webcountry: WebCountry) {
    if (this.IDToShow === webcountry.WebCountryID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(webcountry: WebCountry) {
    if (this.IDToShow === webcountry.WebCountryID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(webcountry: WebCountry) {
    if (this.IDToShow === webcountry.WebCountryID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webcountry.WebCountryID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(webcountry: WebCountry) {
    if (this.IDToShow === webcountry.WebCountryID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webcountry.WebCountryID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetWebCountryList() {
    this.sub = this.webcountryService.GetWebCountryList().subscribe();
  }

  DeleteWebCountry(webcountry: WebCountry) {
    this.sub = this.webcountryService.DeleteWebCountry(webcountry).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesWebCountryText(this.webcountryService.webcountryTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
