/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { WebRootService } from './webroot.service';
import { LoadLocalesWebRootText } from './webroot.locales';
import { Subscription } from 'rxjs';
import { WebRoot } from '../../../models/generated/WebRoot.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-webroot',
  templateUrl: './webroot.component.html',
  styleUrls: ['./webroot.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebRootComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public webrootService: WebRootService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(webroot: WebRoot) {
    if (this.IDToShow === webroot.WebRootID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(webroot: WebRoot) {
    if (this.IDToShow === webroot.WebRootID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(webroot: WebRoot) {
    if (this.IDToShow === webroot.WebRootID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webroot.WebRootID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(webroot: WebRoot) {
    if (this.IDToShow === webroot.WebRootID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webroot.WebRootID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetWebRootList() {
    this.sub = this.webrootService.GetWebRootList().subscribe();
  }

  DeleteWebRoot(webroot: WebRoot) {
    this.sub = this.webrootService.DeleteWebRoot(webroot).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesWebRootText(this.webrootService.webrootTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
