/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { WebSamplingPlanService } from './websamplingplan.service';
import { LoadLocalesWebSamplingPlanText } from './websamplingplan.locales';
import { Subscription } from 'rxjs';
import { WebSamplingPlan } from '../../../models/generated/WebSamplingPlan.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-websamplingplan',
  templateUrl: './websamplingplan.component.html',
  styleUrls: ['./websamplingplan.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebSamplingPlanComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public websamplingplanService: WebSamplingPlanService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(websamplingplan: WebSamplingPlan) {
    if (this.IDToShow === websamplingplan.WebSamplingPlanID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(websamplingplan: WebSamplingPlan) {
    if (this.IDToShow === websamplingplan.WebSamplingPlanID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(websamplingplan: WebSamplingPlan) {
    if (this.IDToShow === websamplingplan.WebSamplingPlanID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = websamplingplan.WebSamplingPlanID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(websamplingplan: WebSamplingPlan) {
    if (this.IDToShow === websamplingplan.WebSamplingPlanID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = websamplingplan.WebSamplingPlanID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetWebSamplingPlanList() {
    this.sub = this.websamplingplanService.GetWebSamplingPlanList().subscribe();
  }

  DeleteWebSamplingPlan(websamplingplan: WebSamplingPlan) {
    this.sub = this.websamplingplanService.DeleteWebSamplingPlan(websamplingplan).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesWebSamplingPlanText(this.websamplingplanService.websamplingplanTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
