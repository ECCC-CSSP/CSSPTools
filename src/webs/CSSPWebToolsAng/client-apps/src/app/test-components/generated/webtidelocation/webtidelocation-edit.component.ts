/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy, Input } from '@angular/core';
import { WebTideLocationService } from './webtidelocation.service';
import { LoadLocalesWebTideLocationText } from './webtidelocation.locales';
import { Subscription } from 'rxjs';
import { WebTideLocation } from '../../../models/generated/WebTideLocation.model';
import { FormBuilder, Validators, FormGroup } from '@angular/forms';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-webtidelocation-edit',
  templateUrl: './webtidelocation-edit.component.html',
  styleUrls: ['./webtidelocation-edit.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebTideLocationEditComponent implements OnInit, OnDestroy {
  sub: Subscription;
  webtidelocationFormEdit: FormGroup;
  @Input() webtidelocation: WebTideLocation = null;
  @Input() httpClientCommand: HttpClientCommand = HttpClientCommand.Put;

  constructor(public webtidelocationService: WebTideLocationService, private fb: FormBuilder) {
  }

  GetPut() {
    return this.httpClientCommand == HttpClientCommand.Put ? true : false;
  }

  PutWebTideLocation(webtidelocation: WebTideLocation) {
    this.sub = this.webtidelocationService.PutWebTideLocation(webtidelocation).subscribe();
  }

  PostWebTideLocation(webtidelocation: WebTideLocation) {
    this.sub = this.webtidelocationService.PostWebTideLocation(webtidelocation).subscribe();
  }

  ngOnInit(): void {
    this.FillFormBuilderGroup(this.httpClientCommand);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }

  FillFormBuilderGroup(httpClientCommand: HttpClientCommand) {
    if (this.webtidelocation) {
      let formGroup: FormGroup = this.fb.group(
        {
          TideLocationList: [
            {
              value: this.webtidelocation.TideLocationList,
              disabled: false
            }, [Validators.required]],
        }
      );

      this.webtidelocationFormEdit = formGroup
    }
  }
}
