/*
 * Auto generated from C:\CSSPTools\src\codegen\_package\netcoreapp3.1\AngularComponentsGenerated.exe
 *
 * Do not edit this file.
 *
 */

import { Component, OnInit, OnDestroy, ChangeDetectionStrategy } from '@angular/core';
import { WebTideLocationService } from './webtidelocation.service';
import { LoadLocalesWebTideLocationText } from './webtidelocation.locales';
import { Subscription } from 'rxjs';
import { WebTideLocation } from '../../../models/generated/WebTideLocation.model';
import { HttpClientService } from '../../../services/http-client.service';
import { Router } from '@angular/router';
import { HttpClientCommand } from '../../../enums/app.enums';

@Component({
  selector: 'app-webtidelocation',
  templateUrl: './webtidelocation.component.html',
  styleUrls: ['./webtidelocation.component.css'],
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class WebTideLocationComponent implements OnInit, OnDestroy {
  sub: Subscription;
  IDToShow: number;
  showType?: HttpClientCommand = null;

  constructor(public webtidelocationService: WebTideLocationService, private router: Router, private httpClientService: HttpClientService) {
    httpClientService.oldURL = router.url;
  }

  GetPutButtonColor(webtidelocation: WebTideLocation) {
    if (this.IDToShow === webtidelocation.WebTideLocationID && this.showType === HttpClientCommand.Put) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  GetPostButtonColor(webtidelocation: WebTideLocation) {
    if (this.IDToShow === webtidelocation.WebTideLocationID && this.showType === HttpClientCommand.Post) {
      return 'primary';
    }
    else {
      return 'basic';
    }
  }

  ShowPut(webtidelocation: WebTideLocation) {
    if (this.IDToShow === webtidelocation.WebTideLocationID && this.showType === HttpClientCommand.Put) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webtidelocation.WebTideLocationID;
      this.showType = HttpClientCommand.Put;
    }
  }

  ShowPost(webtidelocation: WebTideLocation) {
    if (this.IDToShow === webtidelocation.WebTideLocationID && this.showType === HttpClientCommand.Post) {
      this.IDToShow = 0;
      this.showType = null;
    }
    else {
      this.IDToShow = webtidelocation.WebTideLocationID;
      this.showType = HttpClientCommand.Post;
    }
  }

  GetPutEnum() {
    return <number>HttpClientCommand.Put;
  }

  GetPostEnum() {
    return <number>HttpClientCommand.Post;
  }

  GetWebTideLocationList() {
    this.sub = this.webtidelocationService.GetWebTideLocationList().subscribe();
  }

  DeleteWebTideLocation(webtidelocation: WebTideLocation) {
    this.sub = this.webtidelocationService.DeleteWebTideLocation(webtidelocation).subscribe();
  }

  ngOnInit(): void {
    LoadLocalesWebTideLocationText(this.webtidelocationService.webtidelocationTextModel$);
  }

  ngOnDestroy() {
    this.sub?.unsubscribe();
  }
}
